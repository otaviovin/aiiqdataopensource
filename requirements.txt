# Web framework
# Used to create the web server and handle web routes
Flask==3.1.0
Flask-WTF==1.2.1  # Form validation and CSRF protection for Flask

# LLMs and AI
# Libraries for interacting with language models and vector databases
openai==1.63.0  # OpenAI's official Python library
langchain>=0.1.0  # Framework to build apps powered by LLMs
langchain-core==0.2.43  # Core components of LangChain
langchain-openai>=0.1.0  # LangChain's integration with OpenAI
langchain-community>=0.0.17  # Community-contributed integrations for LangChain
chromadb>=0.4.22  # Vector database used with LLMs
docling==2.28.4  # Tool to process and convert documents (e.g., PDF to Markdown)
docling-core[chunking]==2.24.1  # Docling core module with document chunking

# NLP and text manipulation
# For fuzzy matching and string similarity comparisons
fuzzywuzzy==0.18.0
python-Levenshtein==0.12.2  # Required for performance improvements in fuzzywuzzy

# Data and visualization
# Libraries for data analysis, plots, and visual reports
pandas==2.2.3  # Data manipulation and analysis
seaborn==0.13.2  # Statistical data visualization
reportlab==4.0.6  # PDF generation
Pillow==10.3.0  # Image processing (required by reportlab or for QR code generation)

# Backend and integrations
# Database access, payments, and environment variable management
pymongo==4.11.1  # MongoDB client
stripe==8.7.0  # Stripe payment integration
python-dotenv==1.0.1  # Load environment variables from .env file

# Alternative frontend / prototyping
# Used for quickly building web interfaces without HTML/CSS
streamlit==1.40.1

# Data validation
# Useful for defining and validating structured data
pydantic==2.7.1

# Google Sheets API
# Used to access and manipulate Google Sheets
gspread==6.2.1
google-auth  # Required for authentication with Google APIs
google-auth-oauthlib  # OAuth library for Google services

# Email API
# Used to send transactional emails (e.g., confirmations, reports)
sendgrid==6.11.0

# WSGI HTTP server for running Flask in production
gunicorn
